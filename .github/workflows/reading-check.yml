name: Chapter three reading checks
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: reading-${{ github.ref }}
  cancel-in-progress: true
jobs:
  reading:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r scripts/requirements-atlas.txt playwright==1.55.0
      - run: npm ci
      - run: npm run check
      - run: npm run build:pages
      - run: npm run test:reading
      - run: python -m playwright install --with-deps chromium
      - run: python scripts/build-reading-preview.py
      - run: python scripts/test-reading-browser.py
      - name: Package source for review
        if: always()
        run: |
          mkdir -p test-artifacts
          git archive HEAD --format=tar.gz > test-artifacts/source.tar.gz
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: reading-review
          path: |
            test-artifacts/
            public/reading/
            dist-pages/
          retention-days: 14
