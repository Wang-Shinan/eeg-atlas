{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "tsx scripts/sync-content.ts",
    "assets:atlas": "node scripts/prepare-atlas.mjs",
    "predev": "npm run assets:atlas && npm run assets:reading",
    "dev": "npm run sync:rules && astro dev",
    "prebuild": "npm run assets:atlas && npm run assets:reading",
    "build": "astro build",
    "postbuild": "node scripts/sites-worker.mjs",
    "prebuild:local": "npm run assets:atlas && npm run assets:reading",
    "build:local": "npm run sync:rules && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:editor": "node scripts/smoke-editor-worker.mjs",
    "astro": "astro",
    "sync:rules": "tsx scripts/sync-dataset-rules.ts",
    "prebuild:pages": "npm run assets:atlas && npm run assets:reading",
    "build:pages": "node scripts/build-pages.mjs",
    "assets:reading": "node scripts/prepare-reading.mjs",
    "test:reading": "node scripts/test-reading-math.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.5",
    "tailwindcss": "^4.3.3",
    "unist-util-visit": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/typography": "^0.5.20",
    "@types/node": "^22.0.0",
    "gray-matter": "^4.0.3",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
